<div class="row mx-auto mt-5">
  <div class="col-md-6 mx-auto mb-5">
    <div class="card card-body">
      <h1 class="text-center mt-3">登入會員</h1>
      {{> messages}}
      <form action="/users/login" method="POST">
        <div class="mb-3">
          <label for="email" class="mb-2"><span class="text-danger">*</span> Email</label>
          <input
            type="email"
            id="email"
            name="email"
            class="form-control"
            placeholder="請輸入Email"
          />
        </div>
        <div class="mb-3">
          <label for="password" class="mb-2"><span class="text-danger">*</span> 密碼</label>
          <input
            type="password"
            id="password"
            name="password"
            class="form-control"
            placeholder="請輸入密碼"
          />
        </div>
        <button type="submit" class="btn btn-success btn-block">登入</button>
      </form>
      <p class="lead mt-4">
        還沒有會員帳號嗎?
        <a href="/users/register">註冊</a>
      </p>
    </div>
  </div>
</div>