<div class="row mx-auto mt-5">
  <div class="col-md-6 mx-auto mb-5">
    <div class="card card-body">
      <h1 class="text-center mt-3">註冊會員</h1>
      {{> messages}}
      <form action="/users/register" method="POST">
        <div class="mb-3">
          <label for="name" class="mb-2"><span class="text-danger">*</span> 會員名稱</label>
          <input
            type="text"
            id="name"
            name="name"
            class="form-control"
            placeholder="請輸入名稱"
            value="{{name}}"
            required
          />
        </div>
        <div class="mb-3">
          <label for="email" class="mb-2"><span class="text-danger">*</span> Email</label>
          <input
            type="email"
            id="email"
            name="email"
            class="form-control"
            placeholder="請輸入Email"
            value="{{email}}"
            required
          />
        </div>
        <div class="mb-3">
          <label for="password" class="mb-2"><span class="text-danger">*</span> 密碼</label>
          <input
            type="password"
            id="password"
            name="password"
            class="form-control"
            placeholder="請輸入密碼"
            value="{{password}}"
            required
          />
        </div>
        <div class="mb-3">
          <label for="confirmPassword" class="mb-2"><span class="text-danger">*</span> 再次輸入密碼</label>
          <input
            type="password"
            id="confirmPassword"
            name="confirmPassword"
            class="form-control"
            placeholder="請再次輸入密碼"
            value="{{confirmPassword}}"
            required
          />
        </div>
        <button type="submit" class="btn btn-success btn-block">註冊</button>
        <a href="/auth/facebook" class="btn btn-info btn-block"><strong>Facebook </strong>登入</a>
        <a href="/auth/google" class="btn btn-danger btn-block"><strong>Google </strong>登入</a>
      </form>
      <p class="lead mt-4">
        已經有會員帳號嗎?
        <a href="/users/login">登入</a>
      </p>
    </div>
  </div>
</div>